# 표 내부 이미지 처리 방법

## 🖼️ 문제 상황

### **시나리오: 표 안에 그림/이미지가 포함된 경우**

```
┌──────────┬──────────┬──────────┐
│  항목    │  도면    │  규격    │
├──────────┼──────────┼──────────┤
│  철근     │ [IMAGE]  │  D13     │ ← 도면이 있는 셀
│          │ (도식)    │          │
├──────────┼──────────┼──────────┤
│  배근도   │ [IMAGE]  │  상세도    │ ← 이미지가 주 내용
│          │ (배근도)  │          │
└──────────┴──────────┴──────────┘
```

**건설 시방서에서 흔한 케이스:**
- 배근도, 단면도, 상세도
- 제품 사진
- 기호/심볼
- 다이어그램

---

## 🔍 현재 알고리즘의 처리 방식

### **1단계: YOLO 표 감지**
```python
# 표 전체 영역 감지 (이미지 포함)
detector.detect_tables(image_path)

# 결과:
{"bbox": [100, 200, 800, 600]}  # ← 이미지 포함한 표 전체
```
✅ **문제없음** - 표 전체를 감지하므로 내부 이미지 영향 없음

---

### **2단계: OCR 텍스트 추출**
```python
# 표 영역에서 OCR 수행
ocr.extract_text_from_region(image_path, bbox)

# 이미지가 있는 셀에서는?
# 결과:
[
    {"text": "항목", "center": [150, 120]},
    {"text": "도면", "center": [400, 120]},
    {"text": "규격", "center": [650, 120]},
    {"text": "철근", "center": [150, 220]},
    # 이미지 부분: OCR이 아무것도 인식 못함 → 빈 셀
    {"text": "D13",  "center": [650, 220]},
]
```
⚠️ **문제 발생** - 이미지 부분은 OCR이 텍스트로 인식하지 못함

---

### **3단계: 좌표 분석**
```python
# 행 분류
lines = cluster_texts_by_line(text_data)

# 결과:
[
    ["항목", "도면", "규격"],  # 행0
    ["철근", "D13"]            # 행1 - 열 개수 불일치!
    # ↑ 이미지 셀이 빠져서 2개만 인식
]
```
❌ **문제 심각** - 열 개수가 맞지 않음

---

## 🎯 처리 결과 (3가지 케이스)

### **Case 1: 이미지 셀이 완전히 비어있는 경우**
```
원본 표:
| 항목 | [이미지] | 규격 |
| 철근 | [배근도] | D13  |

OCR 인식:
| 항목 |          | 규격 |  ← 이미지 셀 빈칸
| 철근 |          | D13  |

파싱 결과:
[
    ["항목", "", "규격"],  ← 빈 문자열로 처리
    ["철근", "", "D13"]
]
```
✅ **결과: 성공** - 빈 셀로 처리되어 구조 유지됨

---

### **Case 2: 이미지에 텍스트 레이블이 있는 경우**
```
원본 표:
| 항목 | [이미지]      | 규격 |
| 철근 | "배근도"      | D13  |  ← 이미지 아래 텍스트
      | [도면이미지]  |

OCR 인식:
| 항목 | 배근도        | 규격 |  ← 텍스트 인식됨
| 철근 |               | D13  |

파싱 결과:
[
    ["항목", "배근도", "규격"],  ← 텍스트만 인식
    ["철근", "", "D13"]          ← 이미지 부분은 빈칸
]
```
✅ **결과: 부분 성공** - 텍스트 레이블은 추출, 이미지는 손실

---

### **Case 3: 이미지가 텍스트와 겹치거나 노이즈가 많은 경우**
```
원본 표:
| 항목 | [복잡한도면] | 규격 |
| 철근 | [선,화살표]  | D13  |  ← OCR이 선/기호를 오인식

OCR 인식:
| 항목 | ㅡㅡ▶ㅣ      | 규격 |  ← 쓰레기 문자 인식
| 철근 | ///|||      | D13  |

파싱 결과:
[
    ["항목", "ㅡㅡ▶ㅣ", "규격"],  ← 의미 없는 문자
    ["철근", "///|||", "D13"]
]
```
⚠️ **결과: 오류** - 노이즈 데이터 포함

---

## 🛠️ 해결 방안

### **방안 1: 이미지 영역 감지 + 메타데이터 저장 (추천) ⭐**

#### 알고리즘:
1. YOLO로 표 내부의 이미지도 감지
2. 이미지 영역을 별도로 저장
3. OCR 결과와 병합

#### 구현:
```python
# Step 1: 표와 이미지 동시 감지
detector = LayoutDetector()

# 표 감지
tables = detector.detect(image_path, target_classes=["table"])

# 이미지/도식 감지
figures = detector.detect(image_path, target_classes=["figure", "image"])

# Step 2: 이미지가 표 안에 있는지 확인
def is_inside_table(figure_bbox, table_bbox):
    fx1, fy1, fx2, fy2 = figure_bbox
    tx1, ty1, tx2, ty2 = table_bbox

    # 이미지가 표 안에 완전히 포함되는지 확인
    return (fx1 >= tx1 and fy1 >= ty1 and
            fx2 <= tx2 and fy2 <= ty2)

# Step 3: 이미지 위치를 셀로 변환
table_bbox = tables[0]['bbox']
embedded_images = []

for fig in figures:
    if is_inside_table(fig['bbox'], table_bbox):
        # 이미지 중심 좌표 계산
        center_x = (fig['bbox'][0] + fig['bbox'][2]) / 2
        center_y = (fig['bbox'][1] + fig['bbox'][3]) / 2

        embedded_images.append({
            "type": "image",
            "bbox": fig['bbox'],
            "center": [center_x, center_y],
            "text": "[IMAGE]"  # 특수 마커
        })

# Step 4: OCR 결과에 이미지 정보 추가
text_data = ocr.extract_text_from_region(image_path, table_bbox)
combined_data = text_data + embedded_images  # 병합!

# Step 5: 통합 파싱
lines = cluster_texts_by_line(combined_data)

# 결과:
[
    ["항목", "도면", "규격"],
    ["철근", "[IMAGE]", "D13"]  # ← 이미지 위치 표시
]
```

#### JSON 출력:
```json
{
  "type": "table",
  "content": {
    "data": [
      ["항목", "도면", "규격"],
      ["철근", "[IMAGE]", "D13"]
    ],
    "embedded_images": [
      {
        "row": 1,
        "col": 1,
        "bbox": [300, 200, 500, 400],
        "image_path": "extracted_images/table_1_cell_1_1.png"
      }
    ]
  }
}
```

**장점:**
- ✅ 이미지 위치 정확히 파악
- ✅ 표 구조 유지
- ✅ 이미지를 별도 파일로 추출 가능
- ✅ JSON에 이미지 메타데이터 포함

**단점:**
- ❌ YOLO가 이미지/도식을 감지해야 함 (추가 클래스 필요)
- ❌ 구현 복잡도 증가

---

### **방안 2: 빈 셀로 처리 + 후처리 (현재 방식)**

#### 알고리즘:
```python
# 1. OCR로 텍스트만 추출
text_data = ocr.extract_text_from_region(image_path, table_bbox)

# 2. 빈 셀 추정
# - 행/열 개수 분석
# - 빈 공간이 있으면 빈 셀로 채움

# 3. 후처리: 빈 셀에 주석 추가
for row in table_data:
    for col, cell in enumerate(row):
        if cell == "" and is_image_cell(row_idx, col):
            row[col] = "[IMAGE]"  # 마커 추가
```

**장점:**
- ✅ 구현 간단
- ✅ 추가 모델 불필요

**단점:**
- ⚠️ 이미지 위치 추정이 정확하지 않을 수 있음
- ⚠️ 이미지 자체는 추출 못함

---

### **방안 3: 이미지 OCR (문자 인식) 시도**

#### 알고리즘:
```python
# 일부 이미지에 텍스트가 있는 경우
# (예: 도면 번호, 제품명 등)

# 1. OCR 수행
results = ocr.extract_text_from_region(image_path, table_bbox)

# 2. 필터링: 신뢰도 낮거나 특수 문자만 있는 경우 제거
filtered = [
    r for r in results
    if r['confidence'] > 0.7 and
       not is_noise(r['text'])  # 쓰레기 문자 제거
]

def is_noise(text):
    # 선, 기호만 있는 경우 노이즈로 판단
    noise_patterns = ['ㅡ', 'ㅣ', '/', '|', '―', '□', '■']
    return all(c in noise_patterns for c in text)
```

**장점:**
- ✅ 이미지 내 텍스트 라벨 추출 가능

**단점:**
- ❌ 순수 이미지는 처리 못함
- ❌ 노이즈 많음

---

## 📊 실전 전략 (권장)

### **단계별 접근:**

```
Phase 1: 현재 상태 유지 (빈 셀 처리)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 이미지 셀은 빈 문자열 ""로 처리
- JSON에 "embedded_images": [] 필드 추가
- 메타데이터로 표시

결과:
{
  "data": [["항목", "", "규격"]],
  "embedded_images": []  # 비어있음
}

장점: 즉시 사용 가능
단점: 이미지 정보 손실


Phase 2: YOLO 이미지 감지 추가 (중기)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- YOLO에 "figure" 클래스 추가
- 표 내부 이미지 위치 감지
- 이미지 파일로 추출 저장

결과:
{
  "data": [["항목", "[IMAGE]", "규격"]],
  "embedded_images": [
    {
      "row": 0,
      "col": 1,
      "image_path": "images/table_1_0_1.png"
    }
  ]
}

장점: 이미지 위치 정확, 추출 가능
단점: YOLO 재학습 또는 모델 추가 필요


Phase 3: 이미지 내용 인식 (장기)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Vision Transformer 모델로 이미지 분류
- "배근도", "단면도", "제품사진" 등 자동 태깅

결과:
{
  "data": [["항목", "[IMAGE: 배근도]", "규격"]],
  "embedded_images": [
    {
      "row": 0,
      "col": 1,
      "image_path": "images/table_1_0_1.png",
      "type": "배근도",
      "confidence": 0.92
    }
  ]
}

장점: 이미지 내용까지 이해
단점: 매우 복잡, 추가 학습 필요
```

---

## 🎯 현재 구현 상태

### **코드 수정 없이 작동:**
```python
# 현재 src/ocr.py와 src/parser.py는
# 이미지 셀을 자동으로 빈 셀로 처리

# 예시:
text_data = [
    {"text": "항목", "center": [100, 50]},
    {"text": "규격", "center": [300, 50]},
    # 이미지 부분은 OCR 결과 없음
    {"text": "철근", "center": [100, 150]},
    {"text": "D13", "center": [300, 150]}
]

lines = cluster_texts_by_line(text_data)
# [
#   [{"text": "항목"}, {"text": "규격"}],
#   [{"text": "철근"}, {"text": "D13"}]
# ]

# 열 개수 불일치 감지 → 빈 셀 추가 (현재 미구현)
# TODO: normalize_column_count() 함수 필요
```

### **개선 필요 사항:**
```python
# src/parser.py에 추가할 함수
def normalize_column_count(table_data):
    """
    열 개수를 통일 (빈 셀 추가)
    """
    max_cols = max(len(row) for row in table_data)

    normalized = []
    for row in table_data:
        if len(row) < max_cols:
            # 부족한 셀을 빈 문자열로 채움
            row_normalized = row + [""] * (max_cols - len(row))
        else:
            row_normalized = row
        normalized.append(row_normalized)

    return normalized
```

---

## ✅ 결론

### **표 안에 이미지가 있으면:**

1. **현재 상태 (Phase 1):**
   - ✅ 이미지 셀은 빈 셀("")로 처리
   - ✅ 표 구조는 유지됨
   - ❌ 이미지 정보는 손실

2. **추천 개선 (Phase 2):**
   - ✅ YOLO로 이미지 영역 감지
   - ✅ 이미지 파일로 추출
   - ✅ JSON에 메타데이터 포함
   - 구현 시간: ~1주

3. **최종 목표 (Phase 3):**
   - ✅ 이미지 내용 자동 분류
   - ✅ "배근도", "단면도" 등 태깅
   - 구현 시간: ~1개월

### **즉시 사용 가능:**
현재 구현으로도 이미지가 있는 표를 처리할 수 있습니다.
다만 이미지 부분은 빈 셀로 표시됩니다.
